[build]
buildCommand = "cd backend && mvn package -DskipTests -q"

[deploy]
startCommand = "java -jar backend/target/*.jar"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
