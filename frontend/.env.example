# Copia este archivo a .env y completa los valores
# .env está en .gitignore — nunca se sube al repo

# URL del backend Spring Boot (Railway)
VITE_API_URL=https://qautomation-production.up.railway.app
